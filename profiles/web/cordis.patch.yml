# 显式声明 /api 信任的远程访问权威（兜底：CLI --trusted-host 经 webStartup 注入的链路暂未生效）。
# dsh 与 dshu 共用本仓库：dsh 命中 dsh 域名；dshu 命中 dshu 域名（2026-08-27 拆分后补录）。
- id: connection
  config:
    trustedHosts:
      - dsh.lan.supplient2026.lol:3080
      - dshu.lan.supplient2026.lol:3080
